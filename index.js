import chalk from "chalk"; // This module is so cool

console.log(chalk.blue("It works"));

const myUnusedVariable = "I'm not used";

if (myUnusedVariable === "I'm not used") {
  console.log("This is always true");
}